---
import "../styles/global.css";
---
<html lang="es">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Sistema de Rifas</title>
    <style>
      @keyframes fadeIn {
        from { opacity: 0; transform: translateY(20px); }
        to { opacity: 1; transform: translateY(0); }
      }

      .hero-content {
        animation: fadeIn 0.8s ease-out;
      }

      .nav-link {
        transition: all 0.3s ease;
      }

      .nav-link:hover {
        transform: translateY(-2px);
      }
    </style>
  </head>
  <body class="min-h-screen bg-gradient-to-br from-rose-50 via-sky-50 to-white text-slate-900">
    <!-- Animated background elements -->
    <div class="fixed inset-0 overflow-hidden pointer-events-none">
      <div class="absolute top-1/4 -left-48 w-96 h-96 bg-rose-200/60 rounded-full blur-3xl animate-pulse"></div>
      <div class="absolute bottom-1/4 -right-48 w-96 h-96 bg-sky-200/60 rounded-full blur-3xl animate-pulse" style="animation-delay: 2s;"></div>
    </div>

    <!-- Navigation Bar -->
    <nav class="relative z-10 bg-white/70 backdrop-blur-xl border-b border-slate-200/70">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex items-center justify-between h-16">
          <!-- Logo -->
          <div class="flex items-center">
            <span class="text-2xl font-black bg-gradient-to-r from-rose-600 via-fuchsia-600 to-sky-600 bg-clip-text text-transparent">
              Sistema de Rifas
            </span>
          </div>

          <!-- Navigation Links -->
          <div class="hidden md:flex items-center space-x-1">
            <a href="#" class="nav-link px-4 py-2 rounded-lg text-slate-700 hover:bg-slate-900/5 hover:text-slate-900 font-semibold">
              Inicio
            </a>
            <a href="/usuario/eventos" class="nav-link px-4 py-2 rounded-lg bg-gradient-to-r from-rose-600 to-fuchsia-600 hover:from-rose-500 hover:to-fuchsia-500 text-white font-bold shadow-lg shadow-rose-500/20">
              Eventos Disponibles
            </a>
            <a href="#" class="nav-link px-4 py-2 rounded-lg text-slate-700 hover:bg-slate-900/5 hover:text-slate-900 font-semibold">
              Sorteos
            </a>
            <a href="#" class="nav-link px-4 py-2 rounded-lg text-slate-700 hover:bg-slate-900/5 hover:text-slate-900 font-semibold">
              Ganadores
            </a>
            <a href="#" class="nav-link px-4 py-2 rounded-lg text-slate-700 hover:bg-slate-900/5 hover:text-slate-900 font-semibold">
              Contacto
            </a>
          </div>

          <!-- Mobile Menu Button -->
          <div class="md:hidden">
            <button id="mobileMenuBtn" class="text-slate-700 hover:text-slate-900 p-2">
              <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
              </svg>
            </button>
          </div>
        </div>
      </div>

      <!-- Mobile Menu -->
      <div id="mobileMenu" class="hidden md:hidden bg-white/80 backdrop-blur-xl border-t border-slate-200/70">
        <div class="px-2 pt-2 pb-3 space-y-1">
          <a href="#" class="block px-3 py-2 rounded-lg text-slate-700 hover:bg-slate-900/5 hover:text-slate-900 font-semibold">
            Inicio
          </a>
          <a href="/usuario/eventos" class="block px-3 py-2 rounded-lg bg-gradient-to-r from-rose-600 to-fuchsia-600 text-white font-bold">
            Eventos Disponibles
          </a>
          <a href="#" class="block px-3 py-2 rounded-lg text-slate-700 hover:bg-slate-900/5 hover:text-slate-900 font-semibold">
            Sorteos
          </a>
          <a href="#" class="block px-3 py-2 rounded-lg text-slate-700 hover:bg-slate-900/5 hover:text-slate-900 font-semibold">
            Ganadores
          </a>
          <a href="#" class="block px-3 py-2 rounded-lg text-slate-700 hover:bg-slate-900/5 hover:text-slate-900 font-semibold">
            Contacto
          </a>
        </div>
      </div>
    </nav>

    <!-- Hero Section -->
    <main class="relative z-10 flex items-center justify-center min-h-[calc(100vh-4rem)]">
      <div class="hero-content max-w-4xl mx-auto px-4 text-center">
        <!-- Main Heading -->
        <h1 class="text-5xl md:text-7xl font-black mb-6 bg-gradient-to-r from-rose-700 via-fuchsia-700 to-sky-700 bg-clip-text text-transparent leading-tight">
          Bienvenido al<br/>Sistema de Rifas
        </h1>

        <!-- Subheading -->
        <p class="text-xl md:text-2xl text-slate-700 mb-12 max-w-2xl mx-auto">
          Participa en sorteos increíbles y gana premios espectaculares
        </p>

        <!-- CTA Buttons -->
        <div class="flex flex-col sm:flex-row gap-4 justify-center items-center">
          <a href="/usuario/eventos" class="w-full sm:w-auto bg-gradient-to-r from-rose-600 to-fuchsia-600 hover:from-rose-500 hover:to-fuchsia-500 text-white font-bold py-4 px-8 rounded-xl shadow-lg shadow-rose-500/25 hover:shadow-2xl hover:shadow-rose-500/30 transform hover:scale-[1.05] active:scale-95 transition-all duration-300 text-lg">
            Ver Eventos Disponibles
          </a>
          <a href="#" class="w-full sm:w-auto bg-white/70 hover:bg-white text-slate-900 font-bold py-4 px-8 rounded-xl border border-slate-200/80 transition-all duration-300 text-lg">
            Conocer Más
          </a>
        </div>

        <!-- Features Grid -->
        <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mt-20">
          <div class="bg-white/70 backdrop-blur-xl rounded-2xl p-6 border border-rose-200/80 hover:border-rose-300 transition-all duration-300 shadow-lg shadow-rose-200/40">
            <div class="text-4xl mb-4">🎯</div>
            <h3 class="text-xl font-extrabold text-slate-900 mb-2">Fácil de Usar</h3>
            <p class="text-slate-700">Interfaz intuitiva y sencilla para participar</p>
          </div>

          <div class="bg-white/70 backdrop-blur-xl rounded-2xl p-6 border border-sky-200/80 hover:border-sky-300 transition-all duration-300 shadow-lg shadow-sky-200/40">
            <div class="text-4xl mb-4">🔒</div>
            <h3 class="text-xl font-extrabold text-slate-900 mb-2">100% Seguro</h3>
            <p class="text-slate-700">Tus datos y transacciones protegidas</p>
          </div>

          <div class="bg-white/70 backdrop-blur-xl rounded-2xl p-6 border border-indigo-200/80 hover:border-indigo-300 transition-all duration-300 shadow-lg shadow-indigo-200/40">
            <div class="text-4xl mb-4">🎁</div>
            <h3 class="text-xl font-extrabold text-slate-900 mb-2">Grandes Premios</h3>
            <p class="text-slate-700">Sorteos con premios increíbles</p>
          </div>
        </div>
      </div>
    </main>

    <!-- Mobile Menu Toggle Script -->
    <script>
      const mobileMenuBtn = document.getElementById('mobileMenuBtn');
      const mobileMenu = document.getElementById('mobileMenu');

      mobileMenuBtn?.addEventListener('click', () => {
        mobileMenu?.classList.toggle('hidden');
      });
    </script>
  </body>
</html>
